-- CONFIGURATION ---------------------------------------------------------------
local gui_OorN = false -- Set to true to enable GUI
local UPDATE_DATE = "15/4/2025"
local UPDATE_MESSAGE = "Update in progress. Please wait until "..UPDATE_DATE
local NEON_COLOR = Color3.fromRGB(0, 255, 255)

-- IMMEDIATE UPDATE CHECK ------------------------------------------------------
if not gui_OorN then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "⚠️ SYSTEM UPDATING ⚠️",
        Text = UPDATE_MESSAGE,
        Duration = 10
    })
    warn(UPDATE_MESSAGE)
    return
end

-- INITIALIZATION --------------------------------------------------------------
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local TS = game:GetService("TweenService")
local RS = game:GetService("RunService")

local player = Players.LocalPlayer
local currentValues = {Jump = 50, Speed = 16, Flight = 50}
local activeStates = {Jump = false, Speed = false, Flight = false}

-- FUTURISTIC GUI CREATION -----------------------------------------------------
local gui = Instance.new("ScreenGui", player.PlayerGui)
gui.Name = "CyberHUD"
gui.ResetOnSpawn = false

local mainFrame = Instance.new("Frame", gui)
mainFrame.Size = UDim2.new(0, 320, 0, 280)
mainFrame.Position = UDim2.new(0, 20, 1, -300)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
mainFrame.BackgroundTransparency = 0.2

-- [Keep all visual elements from previous futuristic design here] --

-- MOVEMENT SYSTEM -------------------------------------------------------------
local flightVelocity, flightGyro

RS.Heartbeat:Connect(function()
    local character = player.Character
    if not character then return end
    
    local humanoid = character:FindFirstChild("Humanoid")
    local root = character:FindFirstChild("HumanoidRootPart")
    
    if humanoid then
        -- Jump System
        humanoid.JumpPower = activeStates.Jump and currentValues.Jump or 50
        
        -- Speed System
        humanoid.WalkSpeed = activeStates.Speed and currentValues.Speed or 16
        
        -- Flight System
        if activeStates.Flight then
            if not root:FindFirstChild("FlightVelocity") then
                flightVelocity = Instance.new("BodyVelocity", root)
                flightVelocity.MaxForce = Vector3.new(4000, 4000, 4000)
                
                flightGyro = Instance.new("BodyGyro", root)
                flightGyro.MaxTorque = Vector3.new(4000, 4000, 4000)
            end
            
            local cam = workspace.CurrentCamera.CFrame
            local moveVec = Vector3.new(
                UIS:IsKeyDown(Enum.KeyCode.D) and 1 or UIS:IsKeyDown(Enum.KeyCode.A) and -1 or 0,
                UIS:IsKeyDown(Enum.KeyCode.Space) and 1 or UIS:IsKeyDown(Enum.KeyCode.LeftShift) and -1 or 0,
                UIS:IsKeyDown(Enum.KeyCode.W) and -1 or UIS:IsKeyDown(Enum.KeyCode.S) and 1 or 0
            )
            flightVelocity.Velocity = cam:VectorToWorldSpace(moveVec) * currentValues.Flight
            flightGyro.CFrame = cam
        else
            if flightVelocity then flightVelocity:Destroy() end
            if flightGyro then flightGyro:Destroy() end
        end
    end
end)

-- [Keep all other GUI elements and systems from previous futuristic design here] --

-- CHARACTER SAFETY SYSTEM -----------------------------------------------------
player.CharacterAdded:Connect(function(character)
    character:WaitForChild("Humanoid").Died:Connect(function()
        activeStates.Flight = false
        if flightKnob then updateToggle(flightKnob, false) end
    end)
end)

print("Cyber systems operational")
